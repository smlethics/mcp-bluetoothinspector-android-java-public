# Contributing

Open an issue before substantial changes. Keep device access permission-based and read-only. Add unit tests for protocol/model changes. Run `gradle testDebugUnitTest assembleDebug` before a pull request. Never commit device identifiers captured from third parties, credentials, private scan datasets, local machine paths or private infrastructure references.
